interface Props { projectName: string; setProjectName: (value: string) => void; status: string; leftOpen: boolean; rightOpen: boolean; onNew: () => void; onOpen: () => void; onSave: () => void; onPng: () => void; onClear: () => void; toggleLeft: () => void; toggleRight: () => void }

export function TopToolbar(props: Props) {
  return <header className="topbar"><div className="brand"><span className="brand-mark">W</span><span>Wireframe Studio</span></div><div className="toolbar-actions"><button onClick={props.onNew}>새 프로젝트</button><button onClick={props.onOpen}>프로젝트 열기</button><button className="primary" onClick={props.onSave}>프로젝트 파일로 저장</button><button onClick={props.onPng}>PNG 내보내기</button><button className="icon-button" title="전체 삭제" onClick={props.onClear}>삭제</button></div><div className="project-meta"><input aria-label="프로젝트 이름" value={props.projectName} onChange={(event) => props.setProjectName(event.target.value)} /><span className="save-status"><i />{props.status}</span><button className="icon-button" title="왼쪽 패널 접기/펼치기" onClick={props.toggleLeft}>{props.leftOpen ? '◀' : '▶'}</button><button className="icon-button" title="오른쪽 속성 패널 접기/펼치기" onClick={props.toggleRight}>{props.rightOpen ? '▶' : '◀'}</button></div></header>
}
