interface Props { projectName: string; setProjectName: (value: string) => void; status: string; shareUrl?: string; leftOpen: boolean; rightOpen: boolean; onNew: () => void; onOpen: () => void; onSave: () => void; onServerSave: () => void; onProjects: () => void; onCopyShare: () => void; onPng: () => void; onClear: () => void; onLogout: () => void; toggleLeft: () => void; toggleRight: () => void }

export function TopToolbar(props: Props) {
  return <header className="topbar"><div className="brand"><span className="brand-mark">W</span><span>Wireframe Studio</span></div><div className="toolbar-actions"><button onClick={props.onNew}>새 프로젝트</button><button onClick={props.onProjects}>내 프로젝트</button><button onClick={props.onOpen}>파일 열기</button><button className="primary" onClick={props.onServerSave}>서버에 저장</button><button onClick={props.onSave}>파일 저장</button><button onClick={props.onPng}>PNG 내보내기</button><button className="icon-button" title="전체 삭제" onClick={props.onClear}>삭제</button></div><div className="project-meta">{props.shareUrl && <div className="share-link"><input aria-label="공유 URL" readOnly value={props.shareUrl} /><button title="공유 URL 복사" onClick={props.onCopyShare}>복사</button></div>}<input aria-label="프로젝트 이름" value={props.projectName} onChange={(event) => props.setProjectName(event.target.value)} /><span className="save-status"><i />{props.status}</span><button className="icon-button" title="왼쪽 패널 접기/펼치기" onClick={props.toggleLeft}>{props.leftOpen ? '◀' : '▶'}</button><button className="icon-button" title="오른쪽 속성 패널 접기/펼치기" onClick={props.toggleRight}>{props.rightOpen ? '▶' : '◀'}</button><button className="icon-button" onClick={props.onLogout}>로그아웃</button></div></header>
}
